# NLP Classification | Script 04: Evaluation
# Author: Sawood Anwar
library(tidymodels); library(yardstick)
# preds <- fit_rf %>% predict(test) %>% bind_cols(test)
# metrics(preds, truth = credibility_label, estimate = .pred_class)
# roc_curve(preds, credibility_label, .pred_1) %>% autoplot()
cat('Evaluation script ready.\n')
