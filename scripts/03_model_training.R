# NLP Classification | Script 03: Model Training
# Author: Sawood Anwar
library(tidymodels); library(ranger); library(kernlab)
# lr  <- logistic_reg() %>% set_engine('glm')
# rf  <- rand_forest(trees = 500) %>% set_engine('ranger') %>% set_mode('classification')
# svm <- svm_rbf() %>% set_engine('kernlab') %>% set_mode('classification')
# wf  <- workflow() %>% add_recipe(rec)
# fit_lr  <- wf %>% add_model(lr)  %>% fit(train)
# fit_rf  <- wf %>% add_model(rf)  %>% fit(train)
# fit_svm <- wf %>% add_model(svm) %>% fit(train)
cat('Model training script ready.\n')
