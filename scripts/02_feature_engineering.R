# NLP Classification | Script 02: Feature Engineering
# Author: Sawood Anwar
library(tidymodels); library(textrecipes)
# rec <- recipe(credibility_label ~ text, data = train) %>%
#   step_tokenize(text) %>%
#   step_stopwords(text) %>%
#   step_tokenfilter(text, max_tokens = 1000) %>%
#   step_tfidf(text)
cat('Feature engineering script ready.\n')
